spring:
  application:
    name: qversity
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/qversity

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
